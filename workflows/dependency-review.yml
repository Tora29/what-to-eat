# 依存パッケージの脆弱性・ライセンスを PR 時に自動チェックするワークフロー
# セキュリティ多層防御の L3 レイヤー（→ .claude/rules/security.md 参照）
# NOTE: プライベートリポジトリでは GitHub Advanced Security が必要なため一時無効化
# name: Dependency Review

# # PR 作成・更新時に実行
# on: [pull_request]

# permissions:
#   contents: read
#   pull-requests: write

# jobs:
#   dependency-review:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4

#       # 依存パッケージの差分をレビューし、脆弱性・ライセンス違反を検出
#       - uses: actions/dependency-review-action@v4
#         with:
#           fail-on-severity: high          # high 以上の脆弱性でワークフローを失敗させる
#           comment-summary-in-pr: always   # レビュー結果を常に PR コメントとして投稿
